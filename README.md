# FactionsConsoleClient

## How to build

### On Linux:
Run: `dotnet publish MinecraftClient -r linux-x64 --no-self-contained -c Release -p:UseAppHost=true -p:IncludeNativeLibrariesForSelfExtract=true -p:DebugType=None`
